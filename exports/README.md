# Exports

Generated publishable artifacts live under `exports/`.

Current layout:

- `exports/site/<cycle>/`
  The frontend-ready site export bundle for a given cycle.

These files are generated from the validated pipeline. They are presentation-oriented exports, not raw data and not frontend source code.
